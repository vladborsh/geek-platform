{
  "$schema": "http://json-schema.org/schema",
  "id": "ProcAdminSchematicsScam",
  "title": "ScamSchema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the SCAM module",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "path": {
      "description": "The path of the folder",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 1
      }
    },
    "project": {
      "description": "Project",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 2
      }
    }
  },
  "required": [
    "name"
  ]
}
